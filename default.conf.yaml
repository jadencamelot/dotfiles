- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.gitconfig:
      path: git/.gitconfig
      force: true
    ~/.config/git:
      path: git

- create:
  - ~/code/personal
  - ~/code/public
  - ~/code/work
