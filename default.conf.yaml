- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.gitconfig: git/.gitconfig
    ~/.config/git: git

- create:
  - ~/code/personal
  - ~/code/public
  - ~/code/work
